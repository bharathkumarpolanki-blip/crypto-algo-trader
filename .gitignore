# Secrets & credentials
.env

# Logs
trading_bot.log

# Runtime state (contains live position data — not for version control)
positions.json

# macOS
.DS_Store

# Python cache
__pycache__/
*.pyc
*.pyo

# ML models (regenerated by training — not for version control)
models/
*.pkl

# SMA bot runtime state
sma_state.json
sma_bot.log

# Webhook bot runtime state (contains live position data — not for version control)
webhook_state.json
webhook_state.json.tmp
webhook_bot.log
